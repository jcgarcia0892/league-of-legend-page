import { TitleAppearsDirective } from './title-appears.directive';

describe('TitleAppearsDirective', () => {
  it('should create an instance', () => {
    const directive = new TitleAppearsDirective();
    expect(directive).toBeTruthy();
  });
});
