import { MoveDetectionDirective } from './move-detection.directive';

describe('MoveDetectionDirective', () => {
  it('should create an instance', () => {
    const directive = new MoveDetectionDirective();
    expect(directive).toBeTruthy();
  });
});
